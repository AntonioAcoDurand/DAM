#include <stdio.h>
#include <stdlib.h>
	int main() {
        int opcion //No es necesario inicialiazar por que el primer uso de la variable es de escritura.
	while (opcion != 4) {
	   //Código a ejecutar
	   printf("Introduce una opción:(4 para salir)\n");
	   scanf(" %d",&opcion);
	
	return EXIT_SUCCESS;
	}
