#include <stdio.h>
#include <stdlib.h>
 
//Autor Antonio Aco Durand
//
//
   // if (pregunta de sí o no) {
	  // Código si respuesta afitmativa
	//}
// Operadores de comparación son:
// * > Mayor
// * < Menor
// * <= menor o iguak
// * >= mayor o igual
//* == igual
//* != Distinto
    int main() {
	    int condición = 68; // El = Es un operador de asignación
		    if ( condición > 50) {
                          // == Es el operador de comparación
			  // 0 es falso en los casos y el numero es verdad
			printf("Condición vale mas que 1\n");
		}   else {
			 printf("Condición vale menos que 1\n");
	        }
		    return EXIT_SUCCESS;
		}
